language: java

jdk:
- oraclejdk8

env:
- PROJECT_DIR=jaxb-missing-rootelement
- PROJECT_DIR=jaxb-unmarshal-string

script:
- cd $PROJECT_DIR && mvn test -B

cache:
  directories:
  - $HOME/.m2/repository